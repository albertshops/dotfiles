---
description: Explicitly turn the current context or a referenced source into Task Master tickets
---

The user explicitly invoked `/to-tickets`. Load the `to-tickets` skill with the skill tool and follow its complete workflow.

Use the current conversation as the source when no argument is supplied. Otherwise, treat the following as the source reference or additional guidance:

$ARGUMENTS
